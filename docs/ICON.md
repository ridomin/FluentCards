# Package Icon

The FluentCards NuGet package uses `icon.png` located at the repository root.

## Requirements
- Size: 128x128 pixels (recommended)
- Format: PNG with transparency
- Content: FluentCards logo or representative icon

## Placeholder
Until a custom icon is created, the package will build without an icon.
To add an icon, place a `icon.png` file in the repository root.
